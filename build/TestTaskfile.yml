version: '3'

tasks:
  create-mocks:
    summary: |
      Generates the mocks for the test-cases in the project.
    cmds:
      - echo 'Generating mocks'
      - echo '-----------------------------------'
      - echo ''

  execute-tests:
    deps: [create-mocks]
    summary: |
      Runs all the test-cases for the project.
    cmds:
      - echo 'Running unit test cases...'
      - echo '-----------------------------------'
      - echo ''
